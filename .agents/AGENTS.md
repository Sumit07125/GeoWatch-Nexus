# Git Operations Rule

Do NOT run `git push`, `git pull`, or other remote repository synchronization commands on behalf of the user. The user prefers to handle all remote Git operations (pushing/pulling to GitHub) manually.
