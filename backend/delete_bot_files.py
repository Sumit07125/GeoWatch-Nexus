import os
import sys

sys.path.append(os.path.join(os.path.dirname(__file__), "Academic", "SEM-VII", "Major_Project", "Code", "backend"))

# Using firebase storage directly instead of gdrive

def delete_all():
    # Skipping because we are moving to Firebase, deleting files not implemented yet.

if __name__ == '__main__':
    delete_all()
